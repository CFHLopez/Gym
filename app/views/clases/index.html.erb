<table class="table">
  <thead>
  <tr>
    <th>Nombre</th>
  </tr>
  </thead>
  <tbody>
  <% @clases.each do |actual| %>
      <tr>
        <td><%= actual.nombre %></td>
        <td><%= link_to 'Mostrar', clase_url(actual) %></td>
        <td> <%= link_to 'Editar', editar_clase_path(actual) %></td>
        <td><%= link_to 'Eliminar', actual, method: :delete, data: {confirm: '¿ESTA SEGURO?'} %></td>
      </tr>
  </tbody>
  <%end%>
</table>
<br><br>
<%= link_to 'NUEVA CLASE', nuevo_clase_path %>
