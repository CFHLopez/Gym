<div class="container">
  <div class="panel-group">
    <div class="panel panel-primary">
      <div class="panel-heading">
  		  <h1><b><%= @plans.nombre %></b></h1>
      </div>
      <big><b>
        <div class="panel-body">
      	  <%= "Precio: " %> 
      	  <%= @plans.precio %>
        </div>
        <div class="panel-body">
          <b><%= "Descripción: " %></b> 
          <%= @plans.desc %>
        </div>
        <div class="panel-body">
          <b><%= "Clase: " %></b> 
          <%= PlanClase.where(plan_id: @plans.id).each do |actual| %>
            <%= actual.plan.plan %>
            <br>
          <% end %>
        </div>
      </b></big>
    </div>
  </div>
  <%= link_to "Volver", Plan, class: "btn btn-primary" %>
</div>
