<div class="container">
  <% suma = 0 %>
  <% @alumnos.each do |a| %>
    <% suma = suma + 1 %>
  <% end %>
  <h1><b><%= "Alumnos/as" %></b></h1>
  <h4><b><p class="text-warning"> Total Alumnos/as: <%= suma %></p></b></h4>
  <h4><b><p class="text-info"> Instrucciones: </p></b></h4>
  <h5>
    <b><p class="text-success"> 1.- Para 'Ver' un alumno/a en detalle, presionar sobre el nombre de el/la alumno/a </p></b>
    <b><p class="text-success"> 2.- Para 'Editar' la informacion de un alumno/a, presionar sobre el boton 'Editar' </p></b>
    <b><p class="text-success"> 3.- Para 'Borrar' un alumno/a, presionar sobre el boton 'Borrar' </p></b>
    <b><p class="text-success"> 4.- Para 'Crear' un/a nuevo/a alumno/a, presionar sobre el boton 'Nuevo/a Alumno/a' </p></b>
    <b><p class="text-danger"> **Si 'Borra' un alumno/a se eliminara de igual manera todos sus pagos y fichas** </p></b>
  </h5>
  <span class="tn btn-warning btn-lg bt pull-leght glyphicon glyphicon-plus"><%= link_to 'Nuevo/a Alumno/a',nuevo_alumno_path %></span>
  <!-- boton buscar aun no configurado
  <a href="#Buscar" class="btn btn-default btn-lg" data-toggle="modal">Buscar</a>
  <div class="modal fade" id="Buscar">
    <div class="modal-dialog">
      <div class="modal-content">
        <div class="modal-header">
          <h2 class="modal-title"><b>Buscar</b></h2>
          <div class="modal-footer">
            <a href="#" data-dismiss="modal" class="btn btn-danger">Cerrar</a>
          </div>
        </div>
      </div>
    </div>
  </div>
  -->
  <table class="table">
    <thead>
      <tr>
        <th>Nombre Alumno/a</th>
        <th>Plan</th>
        <th>Fecha De Pago</th>
      </tr>
    </thead>
    <tbody>
      <% @alumnos.each do |actual| %>
        <tr>
          <td><b><%= link_to(actual.nombre, actual) %></b></td>
          <td><b><%= actual.plan.nombre %></b></td>
          <td><b><%= actual.created_at.strftime("%d")%></b></td>
          <td><%= link_to 'Editar', editar_alumno_path(actual), class: "btn btn-info"%>
          <%= link_to 'Borrar', actual, method: :delete, data: {confirm: '¿Está Seguro?'}, class: "btn btn-danger"%></td>
        </tr>
      <%end%>
    </tbody>
  </table>
  <br><br>
  <%= will_paginate @alumnos, renderer: BootstrapPagination::Rails %>
</div>
